# =============================================================================
# Environment Variables - End-to-End Example
# =============================================================================
# Copy this file to .env and customize as needed
# 
# Note: Most settings are configured in the TOML files and docker-compose.yml
# These environment variables are mainly for overriding secrets and endpoints

# Webhook Connector
WEBHOOK_API_KEY=demo-api-key-e2e-12345

# SurrealDB Credentials
SURREALDB_USERNAME=root
SURREALDB_PASSWORD=root

# Danube Broker (usually don't need to change)
DANUBE_SERVICE_URL=http://danube-broker:6650

# Logging Levels
# Options: trace, debug, info, warn, error
RUST_LOG=info,danube_source_webhook=debug,danube_sink_surrealdb=debug
